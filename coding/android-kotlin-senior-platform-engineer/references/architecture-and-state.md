# Android architecture and state guidance
- Keep business logic outside UI rendering code.
- Make coroutine ownership explicit.
- Keep UI state, domain state, and persistence concerns separate.
- Prefer typed UI state and clear service/repository boundaries.
- Avoid abstraction layers that merely rename existing concepts.
