# Coding Agent Guidance

- Prefer medium reasoning effort for interactive work unless the task is unusually ambiguous or risky.
- Start from skill metadata and only load extra references when the task genuinely needs them.
- Produce compact plans and small diffs.
- Verify assumptions before coding when a framework API or project convention is uncertain.
- Reuse existing tests and fixtures instead of inventing parallel structures.
